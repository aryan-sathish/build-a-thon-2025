# build-a-thon-2025
its a build a thon what more do u want

# How to open the virtual enviornment
venv\Scripts\activate.bat

# needed to run
pip install Flask
python -m pip install "pymongo[srv]"
pip install flask-pymongo

and python 3(website)